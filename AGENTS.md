# AGENTS.md

このファイルは、このリポジトリで作業するコーディングエージェント向けの指針です。
リポジトリ全体に適用します。サブディレクトリに別の `AGENTS.md` がある場合は、そちらの指示を優先してください。

## プロジェクト概要

- プロジェクト名: `shogi-otoshi`
- 実行環境: Node.js / TypeScript
- 現在は初期構築段階です。未確定の仕様やアーキテクチャを推測で固定しないでください。
- 将棋のルールや用語を実装する場合は、コード上の命名を一貫させ、採用したルール上の前提をテストまたはコメントに残してください。

## 作業方針

- 依頼に必要な最小限の変更を行い、無関係なリファクタリングは避けてください。
- 作業前に関連ファイルと既存のテストを確認し、既存の設計・命名・書式を優先してください。
- ユーザーの未コミット変更を保持してください。依頼と無関係な変更を上書き・削除しないでください。
- 新しい依存関係は、標準機能や既存依存で代替できない場合にだけ追加してください。追加理由を説明できる状態にしてください。
- 秘密情報、API キー、個人情報、実データをコミットしないでください。必要な環境変数は `.env.example` にダミー値と説明を追加してください。
- 仕様が曖昧でも安全で局所的な仮定で進められる場合は進め、重要な判断だけを確認してください。

## コード規約

- TypeScript を優先し、公開境界では明示的な型を使用してください。
- `any`、型アサーション、非 null アサーションは安易に使わず、入力検証や型の絞り込みを優先してください。
- 関数とモジュールは責務を小さく保ち、ドメインロジックと UI・I/O を分離してください。
- エラーを握りつぶさず、利用者が対処できるメッセージまたは呼び出し側で判別できるエラーとして扱ってください。
- コメントは処理内容の言い換えではなく、制約、理由、将棋ルール上の判断など「なぜ」を記述してください。
- 新規コードは既存の formatter / linter 設定に従ってください。設定がない段階で独自の書式を大量導入しないでください。

## 将棋ドメインの実装

- 盤面座標、先手・後手、駒種、成り、持ち駒、打ち手の表現は一か所で定義し、文字列や数値を各所へ直書きしないでください。
- 合法手判定と表示・アニメーションなどのプレゼンテーション処理を分離してください。
- 境界条件をテストしてください。特に成り、不成、二歩、打ち歩詰め、行き所のない駒、王手放置、持ち駒の増減を考慮してください。ただし、ゲーム固有の変則ルールが定義された場合はその仕様を優先します。
- ランダム性を使う処理は、テストで乱数源を差し替えられる設計にしてください。

## コマンドと検証

- `package.json` が追加された後は、そこに定義された scripts を正式なコマンドとして扱ってください。
- パッケージマネージャーは lockfile から判断し、異なる lockfile を新規作成しないでください。
- 変更後は、利用可能な範囲で型チェック、lint、テスト、ビルドを実行してください。
- テストは変更箇所に近いものから実行し、最終的に関連する一式を確認してください。
- 実行できなかった検証がある場合は、理由と未確認事項を作業報告に明記してください。

## テスト方針

- バグ修正では、可能な限り先に再現テストを追加してください。
- 正常系だけでなく、不正入力、境界値、状態遷移、同じ操作の繰り返しも確認してください。
- 実装詳細ではなく、外部から観察できる振る舞いを検証してください。
- スナップショットだけに依存せず、重要なルールや状態は明示的に assertion してください。
- 時刻、乱数、ネットワークなど不安定な外部要因をテストから分離してください。

## 完了時の報告

- 何を変更したかを簡潔に説明してください。
- 実行した検証コマンドと結果を記載してください。
- 残課題、既知の制約、判断を要する点があれば明記してください。
- ファイルを参照する場合は、可能ならファイル名と該当行を示してください。

## プルリクエストの出し方

- devブランチを最新にする
- devブランチを基準にfeature/xxxブランチを作成。xxxは実装機能を示した名前。
- feature/xxxブランチはdevブランチへマージする
- 日本語が使える箇所は日本語を使う。

## コミットについて

- codexのai attributeをメッセージに付与してください
